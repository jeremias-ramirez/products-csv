
class ObjectPropertyFunctions {

    /**
     * @param {String} property
     * @return {String}
     */
    static fromCamelCasetoSnakeCase(property) {
        //const acccumProperty = (obj, accum) => {
        //    accum += property.slice(ob)
        //}
        //[...property.matchAll(/[A-Z]/g)].map(obj => 
        //    property.slice(0, ));
    }
}


exports.ObjectPropertyFn = ObjectPropertyFunctions;


